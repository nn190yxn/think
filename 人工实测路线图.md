# 人工实测路线图

**作者：Monkeycode**

这是第一次人工实测用的操作顺序。逐项判据的完整细节在
`.monkeycode/specs/2026-09-15-thought-forge-deepening/windows-verification.md`，
两边不一致时以那份为准。这份解决的是另外三件事：

1. 先做什么、后做什么；
2. 哪些只能靠你的眼睛判（机器判不了）；
3. 发现问题怎么记，我才好改。

---

## 零、开始之前（一次性，约 10 分钟）

### 1. 起应用

```bash
pnpm tauri:dev
```

改代码后秒级重载。**实测期间一直用它，先不要打包**：打包是用来验安装包的，不是用来找 bug 的。

### 2. 准备一个模型平台的密钥（必须）

应用默认把联网全部关死。不配密钥，会诊、蒸馏、检索**一个都试不出来**——界面上会是灰的、点不动。**那不是 bug，是没配。**

要备三样：

| 要填的 | 说明 |
|---|---|
| 平台端点 | 服务商给的 API 地址 |
| 模型名 | 例如 `deepseek-chat` |
| 密钥 | 服务商给的 key |

密钥优先填在界面里（「我」境界里跟「联网 / 模型平台」有关的那一段）。也可以设环境变量 `THOUGHT_FORGE_API_KEY` 作为回退。

密钥只进 Windows 凭据库，**不进数据库**——这是设计，后面 V4 / V15 专门要查它没落库。

### 3. 装上六位种子大师

界面 →「藏」境界（大师与资产）→「大师架子」→ 点 **「安装种子大师包」**。

**首次会诊前必须做一次**，否则没有大师可以站席。项目自带六位，六层各一位。

### 4. 记住两个位置

| | |
|---|---|
| 全部数据 | `%APPDATA%\com.thoughtforge.desktop\forge.db` |
| 备份 | 同一个目录下 |

只读检查器——**只读，可以和正在运行的应用同时开，看多少次都不改数据**：

```powershell
$DbPath = Join-Path $env:APPDATA 'com.thoughtforge.desktop\forge.db'
cd src-tauri
cargo run -p thought-forge-core --example forge_verify -- $DbPath
```

- 退出码 `0` = 没有未过项（"跳过"不算失败）
- 退出码 `1` = 有未过项
- 退出码 `2` = 命令或库路径写错了

输出里每一项都会列出它实际看到的值；未过项写明原因，跳过项写明缺哪份前置数据。

---

## 一、先确认能不能跑起来（约 15 分钟）

**这一段挂了就别往下测**，后面全都依赖它。

| 验证项 | 你做什么 | 通过判据 | 谁判 |
|---|---|---|---|
| V1 | 应用能起来、界面出得来 | 五个境界都能切（观 / 会 / 炼 / 藏 / 我） | 你 |
| V2 | 填平台端点、模型名，启用平台 | 填过的值还在；检查器 `--expect-platform <平台code>` 通过 | 检查器 + 你 |
| V3 | 跑一次模型探针 | 见下面「⚠ V3 现在做不了」 | — |
| V15 | 关掉应用重开 | **不需要重新填密钥** | 你 |
| V4 | 重启后跑检查器，带上密钥扫描 | 密钥没有出现在数据库里 | 检查器 |

密钥扫描这样跑（`--secret` 后面跟**你的密钥本体**；界面填的密钥不会出现在环境变量里，所以别照抄 `$env:` 那套）：

```powershell
cargo run -p thought-forge-core --example forge_verify -- $DbPath --secret 你的密钥
```

### ⚠ V3 模型探针 —— 现在没有入口，先跳过

我核对了代码：后端确实有 `model_probe` 这个命令，但**界面里没有任何按钮调用它**，命令行也没有入口（程序不接收命令行参数）。所以手册让你"跑一次探针"，按现在的代码你**点不到**。

**这不是你的操作问题。** 它挡着两件事：

- V3 本身没有判定对象；
- 检查器的 V5（最新一次模型调用的用途、平台、模型、耗时齐不齐）会一直停在"跳过"。

要修的话就是把这个入口接上，不大。你实测时如果觉得需要它，跟我说。

---

## 二、核心链路（约 40 分钟）—— 这段最可能让你提需求

这两条是这个软件的灵魂。机器只能判"有没有留下记录"，**好不好用、像不像样只能你来判**。

### V6 会诊全链路

1. 确认已装大师包、联网已开、平台已启用
2. 「会」境界：输入一个问题，选角（六层各取一位），发起会诊
3. 逐轮看完，最后看结论

| 机器判（检查器 `--expect-search`） | 只能你判 |
|---|---|
| 每一轮发言都有记录；每次模型调用都有审计；发言锁定了大师与版本 | **发言质量**：有没有答非所问、有没有空话套话、席位分配合不合理、结论能不能拿去用 |

### V12 提示词隔离

某席位自己补充的检索结果，**只有那个席位看得到**，其他席位看不到。

- 机器判：检查器核对席位来源是否归属于那场阵容里的大师
- 你判：提示词内容本身

### V7 蒸馏全链路

1. 「炼」境界：给一份能解析的语料 + 目标大师名，建入库任务
2. 让它跑完六个阶段，产出技能单元，安装为新版本
3. **中途故意中断一次**，看能不能从检查点接着跑

| 机器判（检查器 `--expect-distill`） | 只能你判 |
|---|---|
| 有完成的任务；大师版本升到 2 以上；版本记录的单元数和实际一致 | **续跑手感**：断了之后好不好恢复；**产物像不像那位大师** |

---

## 三、采集与连接器（约 30 分钟）

### V8 采集注入

> 说明：验证手册第三节没有给 V8 列步骤（它只出现在验收清单和云端说明里），这里按验收清单补齐。

三个能力**逐个单独开**，开一个测一个：

| 能力 | 你做什么 | 检查器命令 |
|---|---|---|
| 剪贴板文本 | 开启后复制一段文本 | `--expect-capture clipboard_text` |
| 活动窗口 | 开启后切换几个前台窗口 | `--expect-capture window` |
| 文件 | 加一个**真实存在**的关注目录，在里面新建一个文件 | `--expect-capture file` |

**这项最关键的判据是反向的**：**每个能力关掉之后，确认不再产生新事件。** 这是这项最容易出问题的地方。

### V11 搜索连接器

配地址和密钥 → 跑一次连接器测试 → 再带检索跑一次会诊。

通过：结果有标题、网址、摘要、时间，且留有审计（检查器 `--expect-search`）。

### V13 MCP 工具清单

配一个 MCP 服务器 → 跑一次连接器测试。

- 你判：**工具清单能不能读懂**
- 机器判：调用有没有进快照（检查器 `--expect-mcp`）

### V14 脱敏

**用一个含手机号和新邮箱的问句**发起检索。

通过：发送出去的串和原始问句不一样，且原文里的手机号确实被替换掉了（检查器）。

### V18 可疑来源

- 你判：可疑来源在提示词里有没有被边界标记包住，结论有没有标注依据来源
- 机器判：标记位（检查器 `--expect-flagged`）

这几项一次跑完的命令：

```powershell
cargo run -p thought-forge-core --example forge_verify -- $DbPath `
  --expect-search --expect-mcp --expect-flagged
```

---

## 四、数据安全（约 20 分钟）

| 验证项 | 你做什么 | 通过判据 |
|---|---|---|
| V16 备份 | 界面建一份备份 → **手动把备份文件改坏** → 尝试恢复 | 必须被拒绝，且现有数据一点没变。再用完好的备份恢复，数据一致 |
| V17 迁移前备份 | 这一项要一次真实迁移，手工做比较绕 | 建议交给云端（内核用例已覆盖）。本机做的话：把库退回较低版本，让新版触发迁移，然后确认备份记录里出现 `pre_migration` 那条 |

---

## 五、装机与升级（两项，情况完全不同）

### V9 安装包 —— 交给云端，别在本机折腾

云端工作流 `.github/workflows/verify-thought-forge-windows.yml` 会在 GitHub 的 Windows 真机上自动完成：生成 NSIS 与 MSI、静默安装、启动、卸载，**并确认卸载后用户数据还在**。

**两个前提：**

1. **它跑的是 GitHub 上的代码，不是你电脑上的。** 今天在本机修的 `/tmp` 问题**还没提交推送**，所以**现在触发会红**——会停在"只读检查器自检用例"那一步。想让它有意义，得先把改动提交并推送。
2. **它不需要任何密钥**，工作流里写明用 `--no-sign` 出未签名包。交接文档里"先配好签名密钥再触发云端验收"那句是多余的——配密钥只有 V10 才需要。

触发：GitHub 仓库 `nn190yxn/think` → Actions →「Verify Thought Forge (Windows)」→ Run workflow。约 30 分钟。

### V10 自动升级 —— 现在做不了

缺两样，少一样都动不了：

| 缺什么 | 要做的事 |
|---|---|
| 更新签名密钥 | 生成一对密钥，私钥与口令**留在本机、不要进仓库**；公钥填进 `src-tauri/tauri.conf.json` |
| 一个真实托管地址 | 把更新清单放到能公网访问的地址上，替换 `plugins.updater.endpoints` |

这是全部 18 项里**唯一**需要额外准备工作才能测的。

### 想亲手摸装机版（可选）

```bash
pnpm tauri build
```

生成 NSIS 与 MSI。第一次全量编译最慢，之后只重编改动的部分。

---

## 六、发现问题怎么记（这一节最实用）

你提的每一件事，**先分成两类**——处理方式不同。

### A 类 · 不符合规格（bug）

**怎么判断**：规格里写了 A，实际做出 B。

**怎么写**：

```
界面：会境界 / 圆桌会诊
我做了什么：点了"以此发起会诊"，选了六位大师
我期望：出一份结论卡
实际发生：转了很久，最后一片空白
```

这类改起来最便宜。每修一个，我都会补一条自动用例，让它以后不再复发。

### B 类 · 规格本身要改（需求）

**怎么判断**：规格里没写，是你用完之后觉得"应该这样才对"。

**怎么写**：

```
界面：炼境界 / 蒸馏熔炉
我想要：能一次喂多份语料，而不是一份一份来
为什么：我的资料本来就是按主题分文件夹的
```

这类要先改规格再改代码，比 A 类多一步，但**仍然是在开发模式下改，不需要重新打包**。

### 两类都只需要三样东西

1. **在哪个界面**
2. **你做了什么**（点了哪、输入了什么）
3. **你期望发生什么**（A 类再加：实际发生了什么）

有截图最好，没有也行——按上面三样说清楚就够我定位。

---

## 七、还差什么（诚实清单）

| 项目 | 状态 |
|---|---|
| V1、V2、V4–V8、V11–V18 | 可以在本机做，按上面第一到第四节 |
| V3 模型探针 | **做不了：界面没有入口**，见第一节的说明 |
| V9 的安装与卸载 | 交云端，**需先提交推送** |
| V9 的签名部分 | 做不了（云端工作流用未签名包） |
| V10 自动升级 | **做不了**，缺签名密钥与托管地址 |
| 云端验收工作流 | 从创建至今**从未执行过**，第一次触发以实际结果为准 |

自动化门禁（`pnpm gate:all`）在本机已全绿：前端 129 用例、内核 262 用例、检查器 7 用例、桌面壳 32 用例，两处 clippy 零警告。也就是说，**上面这些待测项都是"功能有没有接通、好不好用"的验证，不是"代码有没有编译过"的验证**。
