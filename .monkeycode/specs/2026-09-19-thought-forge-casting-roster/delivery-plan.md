# 交付规划：假设 Alex 是甲方（2026-09-19，2026-09-21 按新范围修订）

## 〇、范围调整（2026-09-21）

Alex 定调：**这个软件只自己用，不做签名、不急封装**。封装的事等全部完善、能正常使用之后再谈。

所以当下只做三件事：**能用、功能、bug**。原计划里的"出安装包、签名证书、云端 CI"整体移入「暂缓」，不占当下工作量。

## 一、交付视角的缺口（保留分析，标注当下取舍）

| 缺口 | 当下是否要动 |
|---|---|
| 首启不装种子包，新装即空名册 | **要**（已修代码，待真机确认） |
| 体检清单只报不导，没有"去哪里配"的按钮 | **要**（前端小改） |
| 真机验收一项没做（8 项） | **要**（最大的风险敞口） |
| 名册只有 6 位，术与势各只有一位 | **要**（扩到 20 位） |
| 六位大师每人只有 2 条单元、四题是 0 | **要**（补料） |
| 没有安装包、签名证书未知、云端 CI 未跑 | **暂缓**（不改代码就自己用得到） |
| 没有用户手册、诊断包导出、换机说明 | **低优先**（自用可选，出问题再说） |
| 没有检查更新 | **暂缓** |

## 二、当下三条线的任务总量

**去重后的未完成任务：35 项。** 其中暂缓 2 项、低优先 3 项，**当下要做 30 项**。

| 线 | 项数 | 编号 |
|---|---|---|
| **能用**（装上能用、界面不卡人） | 11 | 25.1 首启（代码已完成，待真机）、25.3 体检跳转、16.3–16.8 六项真机验证、16.10 记录结论、26.1 真机验收报告、26.3 缺口回填 |
| **功能**（把内容补厚、把名册扩满） | 12 | 22.1–22.7 补料七项、24.1–24.5 名册五项 |
| **bug**（先验证再修，不是空想） | 3 | 26.2 补料后的真实会诊、28.2 `okada-kazuo` 拼写、28.4 `SeatPicker` 与内核选角同步 |
| **维护清理** | 4 | 28.1 `.gitignore`、28.3 X.3/X.4、旧规格标注归档（随手册一并做） |
| **暂缓（封装相关，以后再说）** | 2 | 16.9 安装包与签名、25.6 出安装包 |
| **低优先（自用可选）** | 3 | 25.2 用户手册、25.4 诊断包、25.5 换机说明 |

去重说明：原 `delivery-plan.md` 与 `tasklist.md` 把 25.x/26.x/28.x 各列了一遍，另有两处同一件事（`SeatPicker` 同步、`okada-kazuo` 拼写）。本文件从此只做汇总与索引，**明细一律以各规格目录的 `tasklist.md` 为准**，不再重复勾选。

## 三、开干顺序（当下）

1. **25.1 首启自检** —— 代码已完成（启动钩子自动装一次、名册非空则跳过、含用例），只剩真机首次启动确认。
2. **25.3 体检项带"去配置"入口** —— 前端小改，本地能验。
3. **清理 28.1 / 28.2** —— 一行 `.gitignore` + 一个拼写决定，成本低、顺手做掉。
4. **P22 补料（你出内容，我做格式化与蒸馏）** —— 这是"功能"里最大的块，也是名册扩充的前置。
5. **P24 名册扩充** —— 按第一批 4 位 → 第二批 5 位 → 第三批 5 位。
6. **P26 真机走查与修 bug** —— 走一遍，发现一个问题就当场修、补用例、回归。发现的问题回填成新任务。

## 四、真正卡住交付的，只有一件事

**没有任何一条需求在真机上走过。** 代码、用例、门禁都是绿的，但界面手感、断网行为、凭据库读写、真实模型链路——一项都没按"人用它"的方式确认过。

这也是唯一只有你能推进的一环（我不动你的密钥、不替你跑付费调用）。我会把走查清单整理成"照着点就行"的步骤，你点完把卡住的地方丢给我，我修。
