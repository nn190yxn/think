# 真机验收报告 · 2026-09-21（首轮：启动与库形状）

作者: Monkeycode

设备：Alex 的 Windows 机器。
程序：`src-tauri\target\debug\thought-forge-desktop.exe`（调试构建，不是安装包）。
做法：启动该程序一次，等它自行建库并装种子大师包，随后结束进程，再用只读检查器核对数据库。
复现命令（只读，不改数据）：

```
cargo run -p thought-forge-core --example forge_verify -- %APPDATA%\com.thoughtforge.desktop\forge.db --secret <任意探针值>
```

## 一、已实测通过（6 项，0 项未过）

| 项 | 结论 |
|---|---|
| S1 库可用且迁移到最新版本 | 当前版本 17，最新版本 17 |
| S2 联网开关与平台配置 | 读得到；默认关闭，平台 0 个，凭据引用 0 条 |
| V2 平台配置与状态一致 | 平台 0 个，无 ready 要求，一致 |
| V4 密钥不落库 | 扫描 360 个文本列，探针密钥未出现 |
| V7 蒸馏产出与版本一致 | **大师 20 位、技能单元 180 条**（首启自动装入成功） |
| V8 采集落库且关闭后不再产生事件 | 四类采集全关、记录 0 条、最近关闭时间无 |

其中 V7 是本轮最关键的一条：它证明「装完就能用」这条前置条件在真机上成立——20 位大师、180 条单元是应用自己装进去的，不是测试桩造出来的。

## 二、待配置后再跑（8 项：V5、V6、V11、V12、V13、V14、V16、V18）

这 8 项都跳过（不是不通过），原因是库里还没有对应记录，需要先做配置与操作：

| 项 | 缺什么才能跑 |
|---|---|
| V5 模型调用审计字段齐全 | 配一次平台与密钥，跑一次连通测试 |
| V6 会诊链路与调用审计 | 跑一次完整会诊 |
| V11 / V12 / V14 检索连通、归属、脱敏 | 配一个检索连接器，跑一次带检索的会诊 |
| V13 连接器类型覆盖与审计 | 配置任意连接器 |
| V16 备份留痕且文件在位 | 在界面建一份备份 |
| V18 外部内容隔离标记 | 跑一次带检索的会诊 |

## 三、未走的人工项

P16.3 至 P16.10 里依赖界面观感的部分（文案、布局、手感、键盘可达）与 P26.2（补料后的真实会诊），需要人在界面上操作，我无法代做。

## 四、缺口回填成新任务（26.3）

1. 配一次模型平台与密钥 → 跑连通测试 → 复查 V5。
2. 跑一次完整会诊 → 复查 V6，并确认「锁定的人坐到他积累最深的题」在真数据上成立。
3. 配一个检索连接器 → 跑带检索的会诊 → 复查 V11 / V12 / V14。
4. 在界面建一份备份 → 复查 V16。
5. 按 V1–V18 剩余清单复核界面观感，发现的问题开新条目。

以上四条（1–4）都只需要 Alex 在界面上点几下；第 5 条需要他的主观判断。
