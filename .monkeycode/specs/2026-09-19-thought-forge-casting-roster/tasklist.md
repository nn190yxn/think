# 思想熔炉 · 点将与大师名册 · 任务清单

范围：P20 点将与六题积累参与选角（本环境可实施并验证）、P21 蒸馏六题收口（依赖模型密钥）、P22 种子六位补料（内容任务，逐位推进）、P23 入口补齐（以界面为主）。名额与招募向导不在本期。

## P20 点将与六题积累参与选角

接手须知（2026-09-19 交接）：内核源文件在工作区是 CRLF 换行，编辑时用**单行锚点**，
多行锚点会因换行符不一致而匹配失败。内核改动本机无法编译验证（没有 Rust 工具链），
所有内核条目的完成标准是 `pnpm gate:core` 通过，而不只是代码写完。

目标：点「待选角」能选人；选定后座位上留痕、可取消；锁定的人坐到他积累最深的那一题；竞争同一席位时料多者优先。

- [x] 20.1 `council/pool.rs` 的 `build()` 给 `Candidate` 增加 `layer_depth`（每题单元数），并补内核断言
- [x] 20.2 `council/select.rs` 的层次排序主键改为该题积累深度，同深度回落到原策略评分
- [x] 20.3 `council/select.rs` 锁定席位落座层改为「积累最深的未覆盖题」，并列按道法术气器势顺序
- [x] 20.4 `src-tauri/src/commands.rs` 的 `master_list` 带上六题积累计数，同步 `src/ipc/commands.ts` 类型与 `client.ts` 桩数据
- [x] 20.5 `src/realms/CouncilRealm.tsx` 的「待选角」改为可点按钮，已坐席位增加「换人」
- [x] 20.6 新增 `src/components/SeatPicker.tsx`：名册网格、六题积累条、本席位契合度、分档排序、判定他会坐哪一题
- [x] 20.7 点将确认后座位上留痕「已点 · 姓名」并可取消，复用既有 `pinned` 通道传给 `council_select`
- [x] 20.8 名额常量 `ROSTER_CAPACITY = 20` 与未招募空位（含获取途径提示）
- [x] 20.9 `src/styles/shell.css` 补点将面板与六题积累条样式，沿用现有面板与胶囊按钮语言
- [x] 20.10 补测试：面板打开、分档排序、确认留痕、取消、空位不可点、Esc 关闭、键盘可达
- [x] 20.11 「待选角」在会诊进行中禁用，并给出「本轮已经开始，换人下次生效」的说明
- [x] 20.12 「藏」补大师包安装入口：先用 `master_validate` 验包并列出未过项，通过后 `master_install`，失败不写入
- [x] 20.13 安装后展示该大师的六题体检（缺题如实标空），并修正空位提示文案为「从文件安装大师包，或在「炼」蒸馏一位」
- [x] 20.14 六题积累补列表等效视图，键盘可达、焦点可见
- [x] 20.15 运行全量门禁（内核 + 两个 crate clippy + 前端三项）
- [ ] 维护（贯穿）：`src/components/SeatPicker.tsx` 的 `deepestLayer` / `layerFit` / `predictedSeats` 是内核选角规则的**界面复刻**（中文分词也对齐了 `scoring::tokens`）。内核规则一改（`council/select.rs` 的落座层与排序），必须同步这里，否则界面预判与真实选角会对不上。

门禁 P20：`cargo test -p thought-forge-core` 全部二进制通过；两个 crate clippy 归零；`pnpm typecheck`、`pnpm test`、`pnpm build` 通过；「锁定的人坐到他积累最深的题」「同席位竞争按积累深度排序」「覆盖下限仍成立」三条断言通过；点将面板七条前端用例通过；安装入口用例通过（校验失败时列出未过项且不写入）；会诊进行中点将不可点的用例通过。

## P21 蒸馏六题收口

目标：蒸馏产出的大师包，六题尽量都有料；确实没有的题如实留空。

- [x] 21.1 `distill/pipeline.rs` 提取阶段后增加六题收口：缺哪题就对那题再做一次定向提取
- [x] 21.2 收口后的空缺作为有效信息保留，六题档案如实呈现
- [x] 21.3 用桩客户端补单测：某题首轮为空时触发定向提取；两次都为空时保留空缺且不报错
- [x] 21.4 运行内核门禁

## P22 种子六位补料

目标：六位种子大师每人六题都有积累；先做一位并验证同题对立真的出现。

- [ ] 22.1 稻盛和夫：扩充语料（覆盖六题的要点摘录），补单元到每题至少一条，四要素齐备
- [ ] 22.2 稻盛和夫：`master.json` 的 `layers` 改为六题覆盖，与单元一致
- [ ] 22.3 `crates/core/tests/seed_packs.rs` 补「每个包六题都有单元」断言
- [ ] 22.4 用补料后的库跑一次真实会诊，人工确认同题对立与碰撞出现
- [ ] 22.5 其余五位按同一规范逐位补齐；同一规范也适用于从文件装入、且六题有缺口的大师
- [ ] 22.6 运行全量门禁并更新 `README.md` 第九节与当前进度
- [ ] 22.7 名册与大师档案里能看到哪些大师六题有缺口（不拦截，只可见）

## P23 入口补齐（2026-09-19 拍板追加）

目标：把审计发现的「有内核、没入口」处补上，使采集、陪练、定时发现不再只进不出、只能设不能用。

- [x] 23.1 采集列表每条记录加「生成录入」：调既有 `intake_create`，给回执，重复生成时提示不重复
- [x] 23.2 陪练「对撞」入口：能发起一次对撞并看到结果（命令 `companion_collide` 已存在，只缺界面）
- [x] 23.3 「定时发现」可配置：是否定时与间隔（命令 `discovery_schedule` 已存在，只缺界面）
- [x] 23.4 接通「连接器超时」：对外请求读 `connector.timeout_secs`，超时作为可读失败原因
- [x] 23.5 补测试并运行全量门禁；更新 `README.md` 与 `wiring-audit.md` 的处置状态

## P24 名册扩到 20 位（规划见同目录 `roster-plan.md`，交付计划里编号为 P27）

- [ ] 24.1 先完成 P22 补料：六位补到每题至少一条单元、四要素齐备
- [ ] 24.2 第一批 4 位：卡尼曼、塞利格曼、塔勒布、德鲁克
- [ ] 24.3 第二批 5 位：契克森米哈伊、达利欧、波特、大野耐一、奥格威
- [ ] 24.4 第三批 5 位：克劳塞维茨、贝索斯、蒙台梭利、王阳明、老子
- [ ] 24.5 每位走完准入门槛：语料入库 → 蒸馏六题收口 → 六题体检无缺口 → 一次真机会诊
- [ ] 24.6 确认 `seed-packs/okada-kazuo` 的 id 拼写（显示名是稻盛和夫）：只记备注，或安排一次迁移修正

## P25 交付可用性（甲方视角，见同目录 `delivery-plan.md`）

- [ ] 25.1 首启自检：确认新装机器上六个种子包自动就位（**代码已完成**：启动钩子自动装一次、名册非空则跳过、含单元用例；**待真机首次启动确认**）
- [ ] 25.2 （低优先·自用可选）用户手册《从安装到第一场会诊》，非技术语言
- [ ] 25.3 六项体检每项加“去配置”按钮，直达对应面板
- [ ] 25.4 （低优先·自用可选）诊断包导出（版本、门禁状态、日志摘要、配置清单；密钥一律排除）
- [ ] 25.5 （低优先·自用可选）换机迁移三步说明（备份 → 新机恢复 → 凭据重录）
- [ ] 25.6 （暂缓·2026-09-21 Alex 定：先不封装签名）出 NSIS 与 WiX 安装包；证书到位就签，否则写清未签名的安装步骤与风险

## P26 真机验收闭环（需在真机执行，结论由我整理）

- [ ] 26.1 走完 P16.3–16.10，结论落成《真机验收报告》
- [ ] 26.2 走完 22.4 补料后的真实会诊
- [ ] 26.3 验收缺口回填成新任务，不留口头结论

## P28 长期维护与清理

- [ ] 28.1 `.gitignore` 补 `.ohmyagent/`
- [ ] 28.2 `okada-kazuo` 拼写处置（备注或迁移）
- [ ] 28.3 X.3/X.4 两条贯穿维护照常执行
- [ ] 28.4 `SeatPicker.tsx` 与内核选角规则同步（改内核必须同步界面）

## 待定项（需拍板后再排）

- 名额超出时是否只提示不拦截（当前设计：只提示）
- 空位是否要显示「推荐招募哪一类人」（依赖 R3.4 的按题缺人统计，建议放 P24）
- 名额与招募向导（依赖 R3.4 与 P20 的名册）
